Usa questo template per *nominare* unartciolo da eliminare. Usa`{{Delete/IT|Motivo}}` per dare un motivo.

Usa `{{Delete|nocat=true}}` per non includere l'articolo in [:Category:Deletion\_Requests](:Category:Deletion_Requests "wikilink").

<noinclude></noinclude>

[Category:Template Documentation/IT](Category:Template_Documentation/IT "wikilink")
